// Core
import styled from 'styled-components';

export default {
    SpinnerContainer: styled.section`
        position: relative;
        width: 100%;
        height: 450px;
        background-color: ${({ theme }) => theme.app};
    `,
};
